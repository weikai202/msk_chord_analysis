# Raw and processed data (may contain PHI / large files)
data/raw/
data/processed/
data/*.rds
data/*.RDS
data/*.csv
data/*.tsv
data/*.txt

# Output figures and tables (reproducible from code)
output/figures/
output/tables/

# docs/figures are generated by 08_export_web_figures.R
# and committed so GitHub Pages can serve them without a build step
# (do NOT add docs/ to .gitignore)

# R session / environment
.Rhistory
.RData
.Rproj.user/
*.Rproj

# OS cruft
.DS_Store
Thumbs.db
